{
    "name": "matches.js",
    "description": "Test if a DOM element matches a given selector",
    "version": "1.0.3",
    "devDependencies": {
        "karma": "~0.8.5"
    },
    "scripts": {
        "test": "./node_modules/.bin/karma start --browsers Firefox --single-run"
    }
}
